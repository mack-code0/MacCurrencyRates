{
  "name": "currencyratesapi",
  "version": "1.0.0",
  "description": "A REST endpoint for returning a JSON object of the latest currency rates",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "Macaulay Uzu",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "express": "^4.17.1"
  }
}
